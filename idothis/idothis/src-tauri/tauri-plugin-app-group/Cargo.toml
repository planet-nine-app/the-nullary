[package]
name = "tauri-plugin-app-group"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-app-group"

[dependencies]
tauri = { version = "2" }
serde = { version = "1", features = ["derive"] }

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
